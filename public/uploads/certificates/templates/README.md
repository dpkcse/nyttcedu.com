# Certificate PDF template

Upload the production blank certificate template here as:

`blank-certificate.pdf`

The PDF generation service also supports overriding this location with the
`CERTIFICATE_TEMPLATE_PATH` environment variable.
